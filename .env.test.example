# Copy to .env.test to run the DB-backed suite (npm run test:db) against a
# DISPOSABLE database — never your dev "knowledge" DB. The suite auto-migrates it.
KNOWLEDGE_DATABASE_URL=postgres://localhost:5433/knowledge_test
